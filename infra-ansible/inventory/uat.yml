---
# inventory/uat.yml
all:
  children:
    uat:
      hosts:
        uat-app-01:
          ansible_host: 192.168.20.10
          ansible_user: deploy
          ansible_port: 22
          ansible_ssh_private_key_file: "{{ ssh_key_path }}"
          app_domain: api-uat.myapp.com
          app_port: 8080
